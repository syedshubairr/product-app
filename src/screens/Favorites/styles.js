import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  noFavorites: {
    flex: 1,
    padding: 20,
    alignItems: 'center',
  },
  noFavoritesText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'black',
  },
  favoriteContainer: {
    paddingHorizontal: 16,
    paddingVertical: 30,
  },
});
