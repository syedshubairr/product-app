import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 30,
    paddingHorizontal: 15,
    borderWidth: 3,
    borderRadius: 16,
    margin: 10,
    borderColor: '#88da9e',
  },
  textStyle: {
    color: 'white',
    fontSize: 20,
    paddingBottom: 12,
  },
});
